<Page
    x:Class="WiFiConnect.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WiFiConnect"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock x:Name="txtTitle" HorizontalAlignment="Center" Text="Dashboard" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="72" FontFamily="Agency FB" Margin="0,25,0,0"/>
            <TextBlock x:Name="txtWelcome" HorizontalAlignment="Center" Text="Happy DAY FNAME" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="48" Margin="0,20,0,0" FontFamily="Agency FB"/>
            <TextBlock x:Name="txtNoTags" HorizontalAlignment="Center" Text="You don't have any tags setup, click below to setup a tag" TextWrapping="Wrap" VerticalAlignment="Top" FontFamily="Agency FB" FontSize="35"/>
            <ListView x:Name="lstAlerts" Height="370" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" SelectionChanged="OnAlertSelected" HorizontalAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate x:Name="AlertListViewItem">
                        <StackPanel Orientation="Vertical">
                            <TextBlock x:Name="_txtAlertID" Text="{Binding AlertID}"/>
                            <TextBlock x:Name="_txtFlowerpotID" Text="{Binding FlowerpotID}"/>
                            <TextBlock x:Name="_txtAlertLevel" Text="{Binding AlertLevel}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

    </Grid>
</Page>
